{
  "name": "wagers-monorepo",
  "private": true,
  "workspaces": [
    "api",
    "client/web",
    "client/mobile"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run start --parallel",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.1.0"
  }
}
